extends CSGCylinder3D

var speed = 1



# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
		pass
#	get_surface_overide_material(0).uv1_offset.y += delta * speed 
